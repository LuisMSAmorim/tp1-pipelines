name: Hello Workflow

on:
  push:
    branches:
      - main

jobs:
  say_hello:
    runs-on: ubuntu-latest
    steps:
      - name: Print success message
        run: echo "Pipeline iniciado com sucesso" 